# Cloudflare Pages Configuration for Static Site
# Worker name: hey

name = "hey"
compatibility_date = "2025-04-01"

# Build configuration
[build]
command = "npm run build"

# Static asset handling
[site]
bucket = "./dist"

# Routes (will be updated when custom domain is configured)
# routes = [
#   { pattern = "chrislyons.boot.industries/*", zone_name = "boot.industries" }
# ]
